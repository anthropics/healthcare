---
description: Extract plain text from a document file — PDF, DOCX, XLSX, PPTX, RTF, or HTML. Use when a binary document needs to be turned into text before analysis.
---

$ARGUMENTS

Follow `${CLAUDE_PLUGIN_ROOT}/skills/doc-extract/SKILL.md`.
