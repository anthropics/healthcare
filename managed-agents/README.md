# managed-agents/

Placeholder for `agent.yaml` templates targeting the Managed Agents API (`/v1/agents`) — the same skills as the plugins, deployed behind a customer's workflow engine instead of an interactive Claude Code session.

Nothing here yet.
